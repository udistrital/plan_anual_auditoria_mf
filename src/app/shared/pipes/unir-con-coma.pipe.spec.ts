import { UnirConComaPipe } from './unir-con-coma.pipe';

describe('UnirConComaPipe', () => {
  it('create an instance', () => {
    const pipe = new UnirConComaPipe();
    expect(pipe).toBeTruthy();
  });
});
